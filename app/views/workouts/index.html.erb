<h1>Shared Workouts </h1>


<div>
    <div>
        <% @workouts.each do |w| %>
        <h3><%= link_to w.name, workout_path(w) %></h3>
        <%= image_tag(w.image, alt: w.name, width: "300px") %>
        <% end %>
    </div>
</div>